import { RequestInfoInterceptor } from './request-info.interceptor';

describe('RequestInfoInterceptor', () => {
  it('should be defined', () => {
    expect(new RequestInfoInterceptor()).toBeDefined();
  });
});
