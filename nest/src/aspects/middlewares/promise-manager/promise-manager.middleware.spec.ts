import { PromiseManagerMiddleware } from './promise-manager.middleware';

describe('PromiseManagerMiddleware', () => {
  it('should be defined', () => {
    expect(new PromiseManagerMiddleware()).toBeDefined();
  });
});
