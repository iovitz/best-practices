generator client {
  provider = "prisma-client-js"
  output   = "../src/modules/global/sqlite/client"
}

datasource db {
  provider = "sqlite"
  url      = "file:../data.db"
}

model User {
  id   Int    @id @default(autoincrement())
  name String
  age  Int
}
